# Loguard Configuration
# Edit using: sudo loguard edit
# Get token from @BotFather
# Get chat_id from @userinfobot

bot_token = ""
chat_id   = ""

# Optional: leave empty to use system hostname
hostname  = ""

# Advanced (no need to change)
max_retries   = 3
retry_delay   = 5
queue_file    = "/var/log/loguard/pending_alerts.jsonl"
log_file      = "/var/log/loguard/alert.log"